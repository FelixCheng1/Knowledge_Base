# 查询收敛验证报告（2026-09-15）

## 结论

本轮完成 T13（次 T07/T08/T10）两道真实查询收敛验证：C01 目录枚举与 M03 政策定向问答均通过，主代理人工核对具体内容。该结果不等同于全 50 题发布验收，也不包含预热性能统计。

## 已落地修复

- T11 停用终态已提交：`6647c69`；停用资料上传新版本和重试导入均返回实际 `409`，HTTP 回归测试已提交：`28cf994`。
- C01：修正目录请求被误判为 `summary`、虚假标题/时间范围污染检索的问题；目录按活动版本逐文档读取封面/标题证据，并规范中文年份。任一活动资料证据读取失败或缺少可核对的封面/标题时明确失败，不静默漏文档或把全文当作封面。
- M03：政策取向与措施问题定向为 `policy`，补全已锁定活动版本的内容摘要，再去重和重排。
- 查询超时：普通查询 `120` 秒、全文摘要 `600` 秒；非正数或非有限值回退默认值。保留阶段日志、失败终态和会话释放；阻塞依赖调用无法强制终止，但返回后会检查取消状态，停止后续摘要/推流，worker 不写终态。

## 验证结果

8 组测试共 `65/65` 通过：service 38、API 6、disabled HTTP 2、routing 5、directory 4、policy 2、timeout 6、repository 2；前端构建此前已通过。

真实查询共 2/2 通过：

- C01：`17199.0ms`，通过；三主体、三份 2026 年一季度报告。
- M03：`41743.9ms`，通过；回答包含“适度宽松”、工具利率 `-0.25` 个百分点、支农支小 `+5000` 亿、民营 `1` 万亿、科技 `+4000` 亿，与原始 PDF 第 2 页（印刷 I）一致。
- 48 条 citation 已逐条核对活动 `document/version/page/block/excerpt` 一致。

结果文件：`tmp/queryfix-C01-M03-20260915.json`。

C01 会话：`da638eebd1c448c583c36731b6a59c3b`；M03 会话：`9baadd29d2fa475daa5da63a47d57eb2`。两者均位于 `finance_eval_20260915_queryfix_sessions/messages/queries` 独立集合；资料和向量只读共享。

## 当前边界与后续

本轮仅验证 C01、M03 两题。剩余全 50 题、人工核验、T11 浏览器流程、版本边界、SSE 浏览器、性能和 T14 仍待完成；DOC/DOCX 按原约定暂缓。历史 M03 卡点不能证明唯一根因，本轮只确认旧 `processing` 状态并完成修复验证。

运行状态：8005 测试服务已关闭；8002 未重启，仍运行旧代码；改动未推送。
